# Clarity Room

Deployable static meditation visualization prototype built with Nexus Vybe Cloud.

Open `index.html` in a browser or serve the repository root with any static file server.

## Checks

```bash
npm test
```
